{
  "name": "banthuoc-api-mcp",
  "version": "1.0.0",
  "description": "MCP server for BanThuoc API — explore endpoints, schemas, and call APIs directly via AI",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "postinstall": "echo 'Run npm run build manually after install'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
